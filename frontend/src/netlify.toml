[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[[redirects]]
  # Redirect all SPA routes to index.html for React router support
  from = "/*"
  to = "/index.html"
  status = 200
